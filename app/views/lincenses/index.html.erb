<p id="notice"><%= notice %></p>

<h1>Listing Lincenses</h1>

<table>
  <thead>
    <tr>
      <th>Employee</th>
      <th>Fechainicio</th>
      <th>Duracion</th>
      <th>Detalle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lincenses.each do |lincense| %>
      <tr>
        <td><%= lincense.employee %></td>
        <td><%= lincense.fechaInicio %></td>
        <td><%= lincense.duracion %></td>
        <td><%= lincense.detalle %></td>
        <td><%= link_to 'Show', lincense %></td>
        <td><%= link_to 'Edit', edit_lincense_path(lincense) %></td>
        <td><%= link_to 'Destroy', lincense, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lincense', new_lincense_path %>
