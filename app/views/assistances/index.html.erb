<p id="notice"><%= notice %></p>

<h1>Listando asistencia</h1>

<table>
  <thead>
    <tr>
      <th>Fecha</th>
      <th>Empleado</th>
      <th>Asiste</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @assistances.each do |assistance| %>
      <tr>
        <td><%= assistance.fecha %></td>
        <td><%= assistance.employee %></td>
        <td><%= assistance.asiste %></td>
        <td><%= link_to 'Mostrar', assistance %></td>
        <td><%= link_to 'Editar', edit_assistance_path(assistance) %></td>
        <td><%= link_to 'Eliminar', assistance, method: :delete, data: { confirm: '¿Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva asistencia', new_assistance_path %>
